# Codex 작업 요청문

너는 이력서 포트폴리오 사이트의 리뷰 결과를 반영해 문구와 구조를 다듬는 개발자다.

## 목표

채용자가 보기에 더 빠르게 강점이 보이도록 포트폴리오 내용을 정리한다.

## 우선 수정 포인트

1. 첫 화면의 자기소개 문구를 좁히기
   - 현재는 `백엔드, 프론트엔드, AI/RAG, 공간 데이터, 운영 자동화`처럼 범위가 너무 넓어 보인다.
   - 주력 역량 1개, 보조 역량 2개 정도로 압축해서 채용 직무가 바로 보이게 수정한다.
   - 예시는 `FastAPI/Spring Boot 기반 백엔드와 AI/RAG 검색 시스템을 구현해 온 개발자입니다.` 같은 방향이 적절하다.

2. 경력 정보 구체화
   - `올포랜드` 경력 설명이 `개발 업무`처럼 너무 추상적이다.
   - 실제 맡은 역할이 보이도록 `백엔드/API 개발`, `R&D`, `디지털 트윈`, `AI 검색 시스템` 같은 식으로 구체화한다.
   - `생산직 2교대`는 삭제하지 말고, 필요하면 제조 현장 이해 경험으로 연결되는 문구로 정리한다.

3. 회사 프로젝트의 신뢰도 강화
   - 프로젝트별로 `기간`, `기여 범위`, `팀 규모`, `역할`이 보이면 좋다.
   - 지금은 구현 내용이 길고 결과가 상대적으로 약해서, 채용자가 실제 기여도를 판단하기 어렵다.
   - 각 프로젝트에 가능한 범위에서 숫자나 명확한 범위를 넣는다.
   - 아래 3개 항목은 사용자가 직접 채운다.
     - `기간: `
     - `기여 범위: `
     - `팀 규모: `
   - `역할`은 현재 프로젝트 설명, 기술 스택, 문제 해결 흐름을 보고 Codex가 판단해서 넣는다.
   - 역할은 다음처럼 짧고 구체적으로 적는다.
     - `백엔드/API 개발`
     - `RAG 검색 흐름 구성`
     - `3D 디지털 트윈 좌표 정합`
     - `Computer Vision 기반 객체 분할`
     - `운영 자동화 및 유지보수`

4. 성과 문구 보강
   - `구성했다`, `검토했다`, `정리했다`보다 결과가 드러나는 문장으로 바꾼다.
   - 예: 응답 속도 개선, 운영 자동화 시간 절감, 다운로드 수, 사용자 평점, 처리량, 배포/운영 경험.
   - 이미 좋은 예시는 `다운로드 1만+`, `평점 4.6` 같은 실제 수치다.

5. 기술 스택 과다 인상 완화
   - 기술 목록이 넓어서 `다 써봤다`는 느낌을 줄 수 있다.
   - `주력`, `실무 사용`, `프로젝트 경험`, `학습/검토`처럼 구분을 주는 방향을 검토한다.

6. 학습 문구 톤 정리
   - `혼자 강의도 계속 찾아보고` 같은 문장은 성실함은 보이지만 이력서 톤으로는 약하다.
   - `업무에 필요한 기술을 별도로 학습하고 프로젝트에 적용해 왔습니다.`처럼 정리된 표현으로 바꾼다.

7. 네비게이션 순서 통일
   - 화면 순서와 헤더 순서가 다르면 소소하지만 완성도가 떨어져 보일 수 있다.
   - 섹션 순서를 실제 화면 흐름과 맞춘다.

8. 인트로 게이트 모바일 확인
   - 첫 진입 화면은 시각적으로 강하지만, 채용자 입장에서는 진입 장벽이 될 수 있다.
   - 모바일에서 문구 잘림이 없는지 확인하고, 필요하면 더 간단한 진입 문구로 바꾼다.

## 기대 결과

- 지원 직무가 첫 화면에서 더 선명하게 보인다.
- 프로젝트 기여도와 성과가 더 신뢰감 있게 읽힌다.
- 과하게 넓어 보이는 인상을 줄이고, 실제 강점을 빠르게 전달한다.

## 작업 원칙

- 요청 범위 밖 파일은 수정하지 않는다.
- API 형식, DB 스키마, 인증 로직은 건드리지 않는다.
- 가능한 한 문구 수정과 구조 정리만 최소 범위로 진행한다.

## 참고 파일

- `src/components/Hero.jsx`
- `src/components/Profile.jsx`
- `src/components/About.jsx`
- `src/components/Skills.jsx`
- `src/components/Projects.jsx`
- `src/components/VisualWork.jsx`
- `src/components/Contact.jsx`
- `src/components/Header.jsx`
- `src/components/IntroGate.jsx`
- `src/data/projects.js`
- `src/data/visualWorks.js`
- `src/data/skills.js`
